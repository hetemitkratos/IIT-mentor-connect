/** Session price charged to student — in paise (₹150 = 15000 paise). Do NOT change for payments. */
export const SESSION_PRICE_PAISE = 15000

/** Session price in rupees — display only (student-facing) */
export const SESSION_PRICE_RS = 150

/** Mentor payout per completed session (₹100). Platform keeps ₹50. */
export const MENTOR_PAYOUT_RS = 100
